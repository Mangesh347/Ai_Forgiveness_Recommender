name: ai_forgiveness_recommender
description: A Flutter app that provides AI-powered forgiveness recommendations.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  video_player: ^2.9.2   
  animated_background: ^2.0.0 
  simple_animations: ^5.0.0  # ✅ added video player

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    # images
    - assets/Recommder_Logo.png
    - assets/menu.png
    - assets/dropdown.jpg
    - assets/copy.png
    - assets/send_button.png
    
    # videos
    - assets/video1.mp4
    - assets/video2.mp4
    - assets/video3.mp4
